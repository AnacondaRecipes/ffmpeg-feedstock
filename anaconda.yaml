anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: FFmpeg/FFmpeg
      use_max_tag: true
      include_regex: ^n\d+(?:\.\d+)+$
      from_pattern: ^n(.+)$
      to_pattern: \1
